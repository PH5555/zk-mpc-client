spring:
  profiles:
    active: local

  data:
    mongodb:
      uri: mongodb://localhost:27017/zkmpc2

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

client:
  id: "1"
